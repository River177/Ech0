version: "3"
services:
  ech0:
    image: ech0:latest  # 使用本地镜像名称
    container_name: ech0
    ports:
      - "6277:6277"
      # - "6278:6278"
    volumes:
      - ./data:/app/data
      - ./backup:/app/backup
    environment:
      - JWT_SECRET="Hello Echos"
    restart: unless-stopped  # 自动重启
